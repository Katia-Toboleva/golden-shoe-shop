import React from 'react';

import { Container } from '@components';
import { CartContainer } from '@containers';

const Cart = () => (
  <Container>
    <CartContainer />
  </Container>
);

export default Cart;
