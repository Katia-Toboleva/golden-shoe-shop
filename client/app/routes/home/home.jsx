import React from 'react';

import { Container } from '@components';
import { HomeContainer } from '@containers';

const Home = () => (
  <Container>
    <HomeContainer />
  </Container>
);

export default Home;
